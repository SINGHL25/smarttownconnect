import 'package:flutter/material.dart';

class DiscussionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Resident Discussions")),
      body: Center(
        child: Text("Coming Soon: Community forums and discussions."),
      ),
    );
  }
}

